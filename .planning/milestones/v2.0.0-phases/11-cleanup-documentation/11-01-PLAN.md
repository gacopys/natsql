---
phase: 11-cleanup-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/materialize/mapper.go
  - internal/cfg/config.go
  - internal/cfg/config_test.go
  - internal/engine/engine.go
  - internal/materialize/materializer.go
  - internal/materialize/materializer_test.go
autonomous: true
requirements: [CLN-01, CLN-02, CLN-05]
user_setup: []

must_haves:
  truths:
    - "extractNestedField handles both `$.field` and `field` path notation"
    - "Config validation rejects index configurations at load time with a clear error"
    - "Stats.LastError field is removed from the Stats struct"
    - "dlqStream parameter is removed from Run() signature"
    - "All Run() callsites are updated to match the new signature"
    - "Build passes and all existing tests pass"
  artifacts:
    - path: "internal/materialize/mapper.go"
      provides: "$. prefix stripping in extractNestedField"
      contains: "strings.TrimPrefix"
    - path: "internal/cfg/config.go"
      provides: "Index config validation error"
      contains: "secondary indexes are not yet supported"
    - path: "internal/engine/engine.go"
      provides: "Stats struct without LastError"
      not_contains: "LastError"
    - path: "internal/materialize/materializer.go"
      provides: "Run function without dlqStream parameter"
      not_contains: "dlqStream"
  key_links:
    - from: "extractNestedField"
      to: "path splitting"
      via: "strings.TrimPrefix stripping $."
    - from: "Config.Validate"
      to: "indexes field validation"
      via: "v.Indexes check with error accumulation pattern"
    - from: "materialize.Run signature change"
      to: "all callsites in materializer_test.go"
      via: "removed dlqStream parameter"
---

<objective>
Remove unused code, add config validation for indexes, and support `$.` prefix notation in field paths.

**Purpose:** Tripwire this phase — clean up the three smallest, most contained code issues first (D-01, D-02, D-05) so the remaining plans operate on already-clean code.

**Output:**
- `mapper.go` — `extractNestedField` strips optional `$.` prefix before splitting
- `config.go` — index config produces validation error at load time
- `engine.go` — `Stats.LastError` field removed
- `materializer.go` — `Run()` function no longer accepts unused `dlqStream` parameter
- `materializer_test.go` — all `Run()` calls updated to new signature
- `config_test.go` — optionally verify index rejection test
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-cleanup-documentation/11-CONTEXT.md

# Decision references for this plan:
# D-01: Strip $. prefix in extractNestedField
# D-02: Validate error when indexes config present
# D-05: Remove 6 dead symbols (SchemaPrefix, ErrSkipAndAck, Stats.LastError, dlqStream param, EncodePKValue, MustInitBucket)
#   NOTE: SchemaPrefix, ErrSkipAndAck, EncodePKValue, MustInitBucket already removed by Phase 08-04.
#   Remaining: Stats.LastError field + dlqStream parameter.

@internal/materialize/mapper.go
@internal/cfg/config.go
@internal/engine/engine.go
@internal/materialize/materializer.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip `$.` prefix in extractNestedField (CLN-01 / D-01)</name>
  <files>internal/materialize/mapper.go</files>
  <read_first>internal/materialize/mapper.go</read_first>
  <action>
    In `internal/materialize/mapper.go`, modify `extractNestedField` function (line ~115) to accept an optional `$.` prefix:

    1. Before the `strings.Split(path, ".")` at line 116, add:
       ```go
       path = strings.TrimPrefix(path, "$.")
       ```

    This is a 1-line change. It makes `extractNestedField` backward-compatible with both `$.field.nested` and `field.nested` notations.

    Verification of the fix:
    - `paths.TrimPrefix` is idempotent — if path doesn't start with `$.`, it returns path unchanged
    - Existing behavior: paths like `user.id` continue to work as before
    - New behavior: paths like `$.user.id` have `$.` stripped before split
    - No import changes needed — `strings` already imported
  </action>
  <verify>
    <automated>grep -n "TrimPrefix" internal/materialize/mapper.go</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "TrimPrefix.*\$" internal/materialize/mapper.go` returns a positive result
    - `go build ./internal/materialize/` passes
    - `go test ./internal/materialize/ -count=1 -timeout 60s` passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add index config validation error (CLN-02 / D-02)</name>
  <files>internal/cfg/config.go</files>
  <read_first>internal/cfg/config.go</read_first>
  <action>
    In `internal/cfg/config.go`, inside the `Validate()` method (around line 150-247), add a check for unsupported index configuration AFTER existing view validation but BEFORE the error return.

    Find the section at line ~241 where all per-view validation is done and `errs` is about to be checked:

    After the closing `}` of the for-loop over `cfg.Views` (line 241), before `if len(errs) == 0` (line 243), add:

    ```go
    // CR-16 / CLN-02: Reject index configurations (not yet supported)
    for i, v := range cfg.Views {
        if len(v.Indexes) > 0 {
            errs = append(errs, fmt.Sprintf("views[%d]: secondary indexes are not yet supported — remove indexes block from config", i))
        }
    }
    ```

    Note: Do NOT use the `prefix` variable from inside the loop. The Indexes check runs in a NEW loop after the main validation, so it uses its own loop variable `i`.

    This follows the established error accumulation pattern (same as existing validation). The error message is user-actionable: tells the user exactly what to do.
  </action>
  <verify>
    <automated>grep -n "not yet supported" internal/cfg/config.go</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "secondary indexes are not yet supported" internal/cfg/config.go` returns a positive result
    - `go build ./internal/cfg/` passes
    - `go test ./internal/cfg/ -count=1 -timeout 60s` passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Remove dead code — Stats.LastError and dlqStream parameter (CLN-05 / D-05)</name>
  <files>internal/engine/engine.go, internal/materialize/materializer.go, internal/materialize/materializer_test.go</files>
  <read_first>
    internal/engine/engine.go (lines 476-510 — Stats struct and Stats() method)
    internal/materialize/materializer.go (lines 100-130 — Run function signature)
    internal/materialize/materializer_test.go (all Run calls)
  </read_first>
  <action>
    Two dead code removals:

    **A. Remove `Stats.LastError` field from engine.go:**
    - In `internal/engine/engine.go` at line ~482, remove the `LastError` field from the `Stats` struct:
      ```go
      // Stats holds operational metrics for the Engine (D-60).
      type Stats struct {
          Started     bool   `json:"started"`
          Goroutines  int    `json:"goroutines"`
          Views       int    `json:"views"`
          HTTPServing bool   `json:"http_serving"`
          LastError   string `json:"last_error,omitempty"`  // ← REMOVE THIS LINE
      }
      ```
    - The `Stats()` method (line 494-510) already does NOT set `LastError`, so no other changes needed there.
    - Search for any other references to `.LastError` in the codebase to ensure nothing else reads this field.

    **B. Remove `dlqStream` parameter from `materialize.Run()`:**
    - In `internal/materialize/materializer.go` at line ~103, change the `Run` function signature to remove `dlqStream jetstream.Stream`:
      ```go
      func Run(ctx context.Context, js jetstream.JetStream, viewCfg *natsql.ViewConfig, bucket jetstream.KeyValue, logger *slog.Logger, drainCh <-chan struct{}) error {
      ```
    - The `dlqStream` parameter is received but NEVER used inside the function body — no dereference, no method calls.
    - Remove the `jetstream` import from materializer.go if it becomes unused after removal. If `jetstream` is used elsewhere in the file, keep the import.

    **C. Update all callsites in `materializer_test.go`:**
    - Search `internal/materialize/materializer_test.go` for all calls to `materialize.Run(...)` or `Run(...)`.
    - Each call currently passes `dlqStream` as the 5th argument. Remove it from each call.
    - Example transformation:
      ```go
      // Before:
      errCh <- Run(matCtx, js, viewCfg, kvb, dlqStream, logger, nil)
      // After:
      errCh <- Run(matCtx, js, viewCfg, kvb, logger, nil)
      ```

    **Note:** Do NOT remove `dlqStream` variables declared in test functions unless they become unused (Go compiler will complain). If a test declares `dlqStream, err := EnsureDLQStream(...)` and the only use was passing it to `Run()`, remove that variable declaration too.
  </action>
  <verify>
    <automated>
      echo "=== Checking LastError removal ===" &&
      grep -c 'LastError' internal/engine/engine.go &&
      echo "=== Checking dlqStream removal from Run ===" &&
      grep 'func Run(' internal/materialize/materializer.go &&
      echo "=== Building ===" &&
      go build ./internal/materialize/ ./internal/engine/ &&
      echo "=== Testing ===" &&
      go test ./internal/materialize/ ./internal/engine/ -count=1 -timeout 120s
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep 'LastError' internal/engine/engine.go` returns 0 (field removed)
    - `grep 'func Run(' internal/materialize/materializer.go` does NOT contain `dlqStream`
    - `go build ./internal/materialize/ ./internal/engine/` passes
    - `go test ./internal/materialize/ -count=1 -timeout 120s` passes
    - `go test ./internal/engine/ -count=1 -timeout 120s` passes
    - `go build ./...` passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config file → Config parser | Config file read from disk, parsed, validated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | T (Tampering) | internal/cfg/config.go — IndexConfig validation | mitigate | Reject unknown/unexpected index config at load time with explicit error per D-02 |
| T-11-02 | S (Spoofing) | internal/materialize/mapper.go — extractNestedField | accept | `$.` prefix stripping has no security impact — it's a cosmetic prefix removal before dot-path splitting |
| T-11-03 | E (Info Disclosure) | internal/engine/engine.go — Stats.LastError | mitigate | Removing LastError prevents potential info leakage of error details through stats endpoint |

No new network endpoints, authentication paths, or data flows introduced. All changes are code cleanup and validation hardening.
</threat_model>

<verification>
```bash
go build ./... && go vet ./... && go test ./internal/materialize/ ./internal/cfg/ ./internal/engine/ -count=1 -timeout 120s
```
</verification>

<success_criteria>
- [ ] `extractNestedField` correctly handles both `$.field` and `field` (CLN-01)
- [ ] Config validation rejects `indexes` with message "secondary indexes are not yet supported" (CLN-02)
- [ ] `Stats.LastError` removed from `Stats` struct (CLN-05)
- [ ] `dlqStream` parameter removed from `materialize.Run()` (CLN-05)
- [ ] All callsites updated, build passes, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/11-cleanup-documentation/11-01-SUMMARY.md`
</output>
