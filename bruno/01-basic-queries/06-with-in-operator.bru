meta {
  name: With IN operator
  type: http
  seq: 6
}

http {
  method: POST
  url: {{base_url}}/api/v1/query
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "sql": "SELECT * FROM users WHERE user_id IN ('abc', 'def')"
  }
}
