meta {
  name: With LIMIT
  type: http
  seq: 4
}

http {
  method: POST
  url: {{base_url}}/api/v1/query
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "sql": "SELECT * FROM users WHERE user_id = 'abc' LIMIT 1"
  }
}
