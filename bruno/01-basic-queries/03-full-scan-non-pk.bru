meta {
  name: Full scan (non-PK WHERE)
  type: http
  seq: 3
}

http {
  method: POST
  url: {{base_url}}/api/v1/query
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "sql": "SELECT * FROM users WHERE name = 'Alice'"
  }
}
