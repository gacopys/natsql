meta {
  name: Query different view
  type: http
  seq: 7
}

http {
  method: POST
  url: {{base_url}}/api/v1/query
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "sql": "SELECT * FROM products WHERE sku = 'SKU-001'"
  }
}
