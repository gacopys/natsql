meta {
  name: Unknown view
  type: http
  seq: 1
}

http {
  method: POST
  url: {{base_url}}/api/v1/query
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "sql": "SELECT * FROM nonexistent_view WHERE id = 'x'"
  }
}
